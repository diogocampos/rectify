{
  "name": "rectify",
  "private": true,
  "scripts": {
    "start": "parcel src/index.html --no-autoinstall --open",
    "build": "rm -rf dist/* && parcel build src/index.html --public-url . --experimental-scope-hoisting"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0"
  },
  "devDependencies": {
    "parcel-bundler": "^1.9.7"
  }
}
